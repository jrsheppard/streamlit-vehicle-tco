-r requirements.txt
pytest>=8.0
# Maintenance scripts only; the deployed app does not import these packages.
requests==2.34.2
scikit-learn==1.9.1
# Optional: the browser smoke test is skipped when Playwright is not installed.
playwright>=1.47
pytest-playwright>=0.5
